version: "3.9"
services:
  hermes:
    network_mode: host
    build:
      context: .
      dockerfile: hermes.Dockerfile
      args:
        TAG: romac/1837-non-standard-port
    image: hermes:0.11.0
    volumes:
      - "hermes-home:/home/hermes"
      - ".:/config:ro"
    command:
      - -c
      - /config/hermes.config
      - start
volumes:
  hermes-home:
    external: true
